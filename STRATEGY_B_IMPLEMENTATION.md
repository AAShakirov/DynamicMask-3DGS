# 🎯 Стратегия B - Реализация завершена

## ✅ Что реализовано

### Компонент (а): SfM Point Filtering
**Файл:** [`strategies/strategy_B_dynamic_filtering/sfm_filtering.py`](strategies/strategy_B_dynamic_filtering/sfm_filtering.py)

- ✅ Загрузка масок для изображений
- ✅ Проекция 3D-точек на камеры
- ✅ Вычисление доли "динамичных" наблюдений
- ✅ Фильтрация точек по порогу
- ✅ Интеграция в `Scene.__init__`

### Компонент (б): Controlled Splitting
**Файл:** [`strategies/strategy_B_dynamic_filtering/controlled_splitting.py`](strategies/strategy_B_dynamic_filtering/controlled_splitting.py)

- ✅ Класс `MaskAwareDensifier` для проекции гауссианов
- ✅ Фильтрация кандидатов для клонирования
- ✅ Фильтрация кандидатов для разделения
- ✅ Модифицированная функция `densify_and_prune_masked`
- ✅ Интеграция в основной цикл обучения

### Компонент (в): Dynamic Gaussian Pruning
**Файл:** [`strategies/strategy_B_dynamic_filtering/gaussian_pruning.py`](strategies/strategy_B_dynamic_filtering/gaussian_pruning.py)

- ✅ Класс `DynamicGaussianTracker` для отслеживания
- ✅ Обновление статистики на каждой итерации
- ✅ Функция `prune_dynamic_gaussians` для удаления
- ✅ Класс `AdaptivePruningScheduler` для планирования
- ✅ Интеграция в основной цикл обучения

## 📁 Модифицированные файлы

### Основные файлы
1. **`train.py`**
   - Импорт компонентов стратегии B
   - Инициализация `MaskAwareDensifier`, `DynamicGaussianTracker`, `AdaptivePruningScheduler`
   - Замена стандартной денсификации на `densify_and_prune_masked`
   - Обновление трекера и периодическое удаление гауссианов

2. **`scene/__init__.py`**
   - Применение SfM фильтрации перед `create_from_pcd`

3. **`scene/cameras.py`**
   - Добавлено поле `dynamic_mask` в класс `Camera`

4. **`scene/dataset_readers.py`**
   - Добавлено поле `mask_path` в `CameraInfo`
   - Автоматическая загрузка путей к маскам

5. **`utils/camera_utils.py`**
   - Загрузка масок из `.npy` файлов
   - Изменение размера масок под разрешение камеры

6. **`arguments/__init__.py`**
   - Новые параметры: `use_strategy_b`, `strategy_b_sfm_threshold`, и т.д.

### Новые файлы
- `strategies/strategy_B_dynamic_filtering/__init__.py`
- `strategies/strategy_B_dynamic_filtering/sfm_filtering.py`
- `strategies/strategy_B_dynamic_filtering/controlled_splitting.py`
- `strategies/strategy_B_dynamic_filtering/gaussian_pruning.py`
- `strategies/strategy_B_dynamic_filtering/README.md`
- `STRATEGY_B_QUICKSTART.md`
- `STRATEGY_B_CHEATSHEET.md`
- `train_strategy_b.sh`
- `train_strategy_b.ps1`

## 🚀 Как использовать

### Быстрый старт

```bash
# 1. Создать маски
python preprocessing/create_mask.py

# 2. Обучение
python train.py -s dataset/your_scene --use_strategy_b
```

### С кастомными параметрами

```bash
python train.py -s dataset/your_scene \
  --use_strategy_b \
  --strategy_b_sfm_threshold 0.5 \
  --strategy_b_densify_threshold 0.3 \
  --strategy_b_prune_threshold 0.7 \
  --strategy_b_prune_min_obs 10
```

### Используя готовый скрипт

```bash
# Linux/Mac
bash train_strategy_b.sh

# Windows PowerShell
.\train_strategy_b.ps1
```

## 📊 Параметры

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `--use_strategy_b` | False | Включить стратегию B |
| `--strategy_b_sfm_threshold` | 0.5 | Порог фильтрации SfM точек (0-1) |
| `--strategy_b_densify_threshold` | 0.3 | Порог блокировки денсификации (0-1) |
| `--strategy_b_prune_threshold` | 0.7 | Порог удаления гауссианов (0-1) |
| `--strategy_b_prune_min_obs` | 10 | Минимум наблюдений для удаления |

**Правило:** Меньшие значения = более агрессивная фильтрация

## 📚 Документация

- **Быстрый старт**: [`STRATEGY_B_QUICKSTART.md`](STRATEGY_B_QUICKSTART.md)
- **Шпаргалка**: [`STRATEGY_B_CHEATSHEET.md`](STRATEGY_B_CHEATSHEET.md)
- **Подробная документация**: [`strategies/strategy_B_dynamic_filtering/README.md`](strategies/strategy_B_dynamic_filtering/README.md)

## 🔍 Что происходит во время обучения

### 1. Инициализация (итерация 0)
```
[Strategy B-a] Filtering SfM points based on dynamic masks...
[Strategy B-a] Points before filtering: 50000
[Strategy B-a] Points removed (dynamic): 5000 (10.0%)
[Strategy B-a] Points kept (static): 45000 (90.0%)
```

### 2. Во время денсификации (итерации 500-15000)
```
[Strategy B-b] Filtered 150 gaussians from densification (on dynamic areas)
```

### 3. Периодическое удаление (каждые 500 итераций после 3000)
```
[Strategy B-c] Pruning 250 dynamic gaussians (threshold=0.7, min_obs=10)
```

## ✨ Ожидаемые результаты

### ✅ Положительные эффекты
- **Меньше артефактов** от движущихся объектов
- **Чище статические области**
- **Меньше гауссианов** (более эффективная модель)
- **Лучше метрики** на статичных частях сцены
- **Стабильнее обучение** (меньше "плохих" гауссианов)

### ⚠️ Компромиссы
- Медленнее обучение (~10-20%)
- Больше GPU памяти для хранения масок
- Может чрезмерно удалить детали при агрессивных параметрах

## 🔧 Настройка под вашу сцену

### Сцена с большим количеством движения
```bash
--strategy_b_sfm_threshold 0.3 \
--strategy_b_densify_threshold 0.2 \
--strategy_b_prune_threshold 0.6
```

### Сцена с небольшим движением
```bash
--strategy_b_sfm_threshold 0.7 \
--strategy_b_densify_threshold 0.4 \
--strategy_b_prune_threshold 0.8
```

## 🧪 Тестирование

### Сравнение с baseline
```bash
# Baseline (без стратегии)
python train.py -s dataset/scene -m output/baseline

# Strategy B
python train.py -s dataset/scene -m output/strategy_b --use_strategy_b

# Сравнение метрик
python metrics.py -m output/baseline
python metrics.py -m output/strategy_b
```

## 📝 Технические детали

### Архитектура решения

```
Стратегия B
├── (а) SfM Filtering        → Удаляет "плохие" точки перед началом
│   └── filter_point_cloud_with_masks()
├── (б) Controlled Splitting → Блокирует создание гауссианов
│   ├── MaskAwareDensifier
│   └── densify_and_prune_masked()
└── (в) Gaussian Pruning     → Удаляет "старые" динамические гауссианы
    ├── DynamicGaussianTracker
    └── prune_dynamic_gaussians()
```

### Зависимости между компонентами

1. **Инициализация** (один раз)
   - SfM Filtering → Фильтрует облако точек

2. **Каждая итерация обучения**
   - Dynamic Tracker → Обновляет статистику

3. **Каждые 100 итераций** (во время денсификации)
   - Controlled Splitting → Фильтрует новые гауссианы

4. **Каждые 500 итераций** (после 3000)
   - Gaussian Pruning → Удаляет динамические гауссианы

## 🎓 Научное обоснование

Стратегия B основана на следующих принципах:

1. **Ранняя фильтрация** - предотвращает инициализацию гауссианов в динамических областях
2. **Контроль роста** - не дает адаптивной денсификации создавать лишние гауссианы
3. **Непрерывная очистка** - удаляет гауссианы, которые "застряли" в динамических областях

## 🙏 Благодарности

Реализация основана на:
- Оригинальной статье 3D Gaussian Splatting (Kerbl et al., 2023)
- Анализе механизма адаптивной денсификации
- Исследованиях по обработке динамических сцен

## 📧 Контакты

Для вопросов и предложений:
- GitHub Issues: [создать issue](../../issues)
- Документация: см. файлы в `strategies/strategy_B_dynamic_filtering/`

---

**Версия:** 1.0  
**Дата:** Декабрь 2025  
**Статус:** ✅ Готово к использованию
